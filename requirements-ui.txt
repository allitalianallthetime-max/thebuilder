# requirements-ui.txt
# Used by: app.py (Streamlit UI)
#
# Render defaults to Python 3.14 — protobuf 5.x is REQUIRED (4.x crashes on 3.14).
# streamlit >= 1.44 ships with protobuf 5.x support and safe HTML rendering.
# The hero landing page uses components.html() as a belt-and-suspenders fallback.

streamlit>=1.44.0,<2.0
httpx
python-dotenv
protobuf>=5.29.0
